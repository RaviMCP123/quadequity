import type { FilterDropdownProps } from "antd/es/table/interface";
import DateFilterDropdown from "./DateFilter";

const DateFilterDropdownComponent: React.FC<FilterDropdownProps> = (props) => (
  <DateFilterDropdown {...props} />
);

export default DateFilterDropdownComponent;
